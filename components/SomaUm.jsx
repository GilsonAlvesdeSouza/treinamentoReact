export default function SomaUm(props) {

    // let val = props.numero
    // for (let index = 0; index < 110; index++) {
    //     val++        
    // }

    return (
        <div>
            <h1>{props.numero + 1}</h1>
        </div>
    )
}